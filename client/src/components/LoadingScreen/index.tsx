import React from "react";

const LoadingScreen = () => {
  return (
    <div
      style={{
        height: "100vh",
        width: "100vw",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h3>Loading...</h3>
    </div>
  );
};

export default LoadingScreen;
